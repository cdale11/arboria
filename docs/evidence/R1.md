# R1 living nursery foundation — aggregate evidence

Date: 2026-09-14.

## Implemented Coverage

- Toolchain and launcher: locked Conda/npm stacks, fingerprinted frontend builds,
  `run.sh`, launcher check mode, repeat/stale-build/path tests.
- Access and command ownership: shared-password auth, sessions, CSRF,
  same-origin unsafe request checks, process lock, world/timeline/request epoch,
  bounded command envelopes, receipts, and stream snapshots/resync.
- Time and lifecycle: server-owned simulation clock, one-writer world loop,
  pause/resume/speed, manual checkpoints, named saves, restore, autosave, and
  paused lifecycle controls.
- Persistence and recovery: SQLite metadata, immutable checkpoint generations,
  startup cleanup, active-checkpoint validation, parent-chain fallback,
  migration for nursery schemas 1-4, atomic-save failure injection, protected
  plant persistence, and current-domain export/import archives.
- Biology and species: organ topology, bounded vegetative growth, bounded
  light-driven carbon assimilation, finite root-zone water and reservoir,
  finite substrate N/P/K uptake, deficiency stress, damage/death, six provisional
  species, and documented species coverage boundaries.
- Nursery, UI, and economy: authenticated plant projections/inspection,
  generated display-only SVG nursery view, desktop and touch-sized controls,
  watering, shop purchases, demand-limited sales, plant sale protection,
  checkpoints/named saves/restore/export/import controls, and finite cash/water
  accounting.
- Companion baseline: deterministic low-water caretaker proposals, authenticated
  settings/run endpoints, companion-owned receipts through the ordinary command
  validator, and browser enable/check controls. Evidence: `docs/evidence/R1-companion.md`.

## Evidence Files

- `docs/evidence/R1-dependencies.md`
- `docs/evidence/R1-launcher-server.md`
- `docs/evidence/R1-launcher-gates.md`
- `docs/evidence/R1-auth-baseline.md`
- `docs/evidence/R1-auth-hardening.md`
- `docs/evidence/R1-process-lock.md`
- `docs/evidence/R1-world-metadata.md`
- `docs/evidence/R1-command-envelope.md`
- `docs/evidence/R1-stream-baseline.md`
- `docs/evidence/R1-stream-resync.md`
- `docs/evidence/R1-clock-baseline.md`
- `docs/evidence/R1-world-loop.md`
- `docs/evidence/R1-lifecycle.md`
- `docs/evidence/R1-checkpoint-layout.md`
- `docs/evidence/R1-named-saves.md`
- `docs/evidence/R1-metadata-restore.md`
- `docs/evidence/R1-metadata-autosave.md`
- `docs/evidence/R1-interrupted-checkpoint-cleanup.md`
- `docs/evidence/R1-active-checkpoint-validation.md`
- `docs/evidence/R1-recovery.md`
- `docs/evidence/R1-organ-topology.md`
- `docs/evidence/R1-vegetative-growth.md`
- `docs/evidence/R1-carbon-assimilation.md`
- `docs/evidence/R1-nursery-water.md`
- `docs/evidence/R1-nutrient-stress.md`
- `docs/evidence/R1-species.md`
- `docs/evidence/R1-living-nursery.md`
- `docs/evidence/R1-economy.md`
- `docs/evidence/R1-ui-controls.md`
- `docs/evidence/R1-companion.md`

## Current Gate Results

Run inside activated Conda `arboria` on the inspected Linux x86-64 host:

```bash
python -m pytest tests/unit
ruff check .
mypy src tests/unit tests/performance
PATH="$CONDA_PREFIX/bin:$PATH" npm --prefix web run check
PATH="$CONDA_PREFIX/bin:$PATH" npm --prefix web run test
PATH="$CONDA_PREFIX/bin:$PATH" npm --prefix web run test:e2e
PATH="$CONDA_PREFIX/bin:$PATH" npm --prefix web run build
```

Results from the latest full gate run:

- `python -m pytest tests/unit`: 150 passed, with the known FastAPI/Starlette
  `TestClient` deprecation warning.
- `ruff check .`: passed.
- `mypy src tests/unit tests/performance`: passed.
- `npm --prefix web run check`: passed.
- `npm --prefix web run test`: 21 passed.
- `npm --prefix web run test:e2e`: 1 passed.
- `npm --prefix web run build`: passed.

## Performance Characterization

Command:

```bash
python -m pytest tests/performance -m performance
```

The performance suite passed 7 tests in 126.35 seconds on the inspected host.
It builds heterogeneous starter nurseries through the public pure biology
kernels and checks finite state and root-zone capacity bounds. The ten-sim-year
soak uses 50 plants because a full 500-plant decade is not yet tractable in the
current pure-Python kernel; it ran 1,051,200 ticks in 150.3 seconds and ended
finite and within capacity, with 50 dead plants after finite unwatered nutrient
and water supplies were exhausted.

Measured single-tick samples from the same host:

| Population | Samples | p50 | p95 | p99 | Result |
| --- | ---: | ---: | ---: | ---: | --- |
| 500 plants / 1,500 organs | 30 | 55.9 ms | 60.3 ms | 62.0 ms | R1 provisional latency target passes |
| 1,000 plants / 3,000 organs | 15 | 170.4 ms | 175.3 ms | 175.3 ms | R6 provisional latency target passes |

The optimization removed redundant whole-world topology validation from three
private per-plant exchange helpers and changed those updates to indexed
in-place replacement while retaining validation at the public
`advance_nursery` boundaries. The observed 500-plant single tick improved from
approximately 2.06 seconds to 0.056 seconds. Indexed organ access removes the
remaining per-plant full-list exchange and assimilation rebuilds; no offscreen
or biological activity was disabled.

The reproducible harness lives in `tests/performance/harness.py` and
`tests/performance/test_performance.py`; no generated timing artifact is
committed.

The full-target command was executed with 500 plants and 10 simulated years
using the same `run_soak` path. The nursery built in 0.74 seconds and the
1,051,200-tick soak completed in 853.22 seconds. The final checkpoint reported
500 plants, 1,500 organs, finite state, valid water capacity, 42.56 MiB peak
RSS, approximately `1e-12 kg` water residual, 10 invariant samples, zero
exceptions, and no stall. All plants were dead at the end because the scenario
provides finite unwatered nutrient and water supplies; this is expected for the
current R1 model rather than a stability failure.

## Release Blockers Still Open

- `[AI/ML]` companion authority, neural learning, learner persistence,
  optimizer/replay/RNG continuation, and learning holdout/ablation evidence are
  not implemented.
- Current-domain export/import does not include learner state because no learner
  exists yet.
- The R1 500-plant latency and ten-sim-year stability targets pass on this host;
  the complete aggregate performance gate remains open because no 24-real-hour
  operational soak has been run.
- No physical touch-device smoke test has been recorded; browser e2e uses the
  locked Vitest/jsdom stack.
- The 24-real-hour operational soak and complete aggregate performance gate
  have not been run.
- Fertilizer items, price forecasting, species calibration, companion behavior,
  and later horticultural mechanics remain deferred.

R1 therefore has a substantial playable foundation, but the roadmap must not be
marked as a complete R1 release until the open AI/ML, physical-device, and
performance/soak gates are satisfied or explicitly rescoped.
